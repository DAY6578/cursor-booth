# DEV_RULES

Cursorは、このリポジトリの唯一の開発担当である。

## 実装してよいもの

GitHub Issueに `cursor-ready` が付いているものだけ。

ラベルがない依頼、チャット上の思いつき、Growthの提案、Supportの再現メモは実装しない。Issue番号を確認し、`cursor-ready` が無ければ作業を止める。

オーナーが口頭で依頼した場合も同じである。先に店長Botが仕様化し、`cursor-ready` が付いてから着手する。

## 実装時

- Issueに書いてある仕様だけを実装する。書いていない機能は足さない。
- WHAT と WHY は Issue に従う。HOW（構成、命名、処理の分け方）は Cursor が決める。
- 既存購入者の設定、ブラウザに残っているデザイン、保存済み時計ファイルの互換を、可能な限り維持する。
- 外部CDN、Node、サーバー、ビルド工程を新しく導入しない。時計はローカルファイルのまま動く。
- 購入者向け zip に `販売ページ/` を入れない。販売者用の文章は購入者に渡さない。
- フォントは同梱のまま使う。ライセンスファイルを消さない。

## 完了時に Issue へ残すもの

- 変更内容
- 変更ファイル
- テスト結果
- 既知の問題
- QA手順（QA Botがブラウザで再現できる手順）

残したあと、`cursor-ready` を外し、`qa-ready` を付ける。

```
gh issue edit <number> --remove-label cursor-ready --add-label qa-ready
```

自分で BOOTH に公開しない。価格を変えない。購入者にメッセージを送らない。

## 完了コメントの型

```
変更内容:
変更ファイル:
テスト結果:
既知の問題:
QA手順:
```

QAが FAIL を返したら、同じ Issue の失敗条件だけを直す。FAILをきっかけに別機能へ広げない。
